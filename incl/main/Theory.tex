\chapter{Transformer}



\section{Recurrent Neural Network and Backpropagation}

A recurrent neural network (RNN) is a type of neural network designed to handle sequential data by generating outputs based on previous inputs, meaning it can retain information as it processes earlier data points in a sequence. A visual example of an RNN can be seen in Figure \ref{fig:example RNN}.
\begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{fig/img/RNN/RNN_Example_Extra.pdf}
    \caption{Example of an RNN}
    \label{fig:example RNN}
\end{figure}

